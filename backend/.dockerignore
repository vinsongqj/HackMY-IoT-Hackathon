# Local virtualenv is rebuilt inside the image from uv.lock
.venv
__pycache__/
*.py[cod]

# Secrets are injected at runtime via `env_file`, never baked into the image
.env
.env.*
!.env.example

# Not needed to run the service
scripts/
